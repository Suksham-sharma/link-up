import { requireUser } from "../lib/hooks/auth-hook";

const Dashboard = async () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <div>Hey there </div>
    </div>
  );
};

export default Dashboard;
